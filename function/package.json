{
  "name": "ai-studio-catalog-function",
  "version": "1.0.0",
  "description": "Azure Function proxy for the AI Studio Agent Catalog",
  "main": "dist/getAgents/index.js",
  "scripts": {
    "build": "tsc && mkdir -p getAgents && cp src/getAgents/function.json getAgents/function.json",
    "watch": "tsc -w",
    "start": "func start",
    "prestart": "npm run build"
  },
  "dependencies": {
    "@azure/functions": "^3.5.1",
    "@azure/msal-node": "^2.6.0"
  },
  "devDependencies": {
    "typescript": "^5.4.5",
    "@types/node": "^20.12.7"
  }
}
